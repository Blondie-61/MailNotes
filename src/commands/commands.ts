/* global Office */

// MailNotes verwendet derzeit ausschließlich ShowTaskpane-Aktionen.
// Outlook erwartet für die FunctionFile trotzdem eine gültige Seite.
Office.onReady(() => {
  // Keine ExecuteFunction-Kommandos erforderlich.
});
